transactions:
  - name: RegisterUser
    steps:
      - action: insert
        table: Users
        values:
          Username: '{{ username }}'
          Password: '{{ password }}'
          Email: '{{ email }}'